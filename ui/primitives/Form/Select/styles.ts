import { BASE_INPUT_HEIGHT } from '../base-styles'
import { StyleType } from './select.type'

export const styles = {
  default: {
    width: '100%',
    bg: '_white',
    h: BASE_INPUT_HEIGHT,
    border: '1px solid',
    borderColor: '_lightgray',
    borderRadius: 'md',
  },
} as StyleType
